# Groot Compiler Project

This is groot compiler (grootc) project repository.

## Install dependencies

### LLVM

```
git clone https://github.com/llvm/llvm-project.git
cmake -S llvm -B build -DCMAKE_BUILD_TYPE=Release
```

